# Problems of current implementation
- I can't create any worktree containing letter `r` because it throws me to `remove` mode mid typing
- When I enter `remove` mode there's no indicator how to get out from there
- It's not readable what is the currently active branch. Let's have a color for that and maybe emoji?
- It's not readable what's the branch-only and what it the already existing worktree
- typing cursor should be blinking to be more obvious it's actually a cursor
- the `(will create)` indicator should be different color than the rest of the text
- the bottom element explaining navigation is not very readable

